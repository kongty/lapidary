workload_1:
  dist: "poisson" # manual or poisson or stream
  dist_start: 0 # Can be random number in a range
  dist_size: 10
  dist_lambda: 300
  tasks:
    task_0:
      app: "app_0"
      dependencies: []
    task_1:
      app: "app_1"
      dependencies: [task_0]
